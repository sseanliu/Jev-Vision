# Training pod for scripts/*.sh (RunPod H100, no network volume; everything is rebuilt under /workspace by
# model/runpod/bootstrap_v5_pod.sh). Fill in your own pod; pods without a network volume lose their disk when stopped.
POD_ID=
POD_HOST=
POD_PORT=
